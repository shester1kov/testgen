# Design Mode Environment Configuration
# This file is used for deploying the app in design mode (for Anima, Figma, etc.)
# Copy this to .env when deploying for design purposes

# API Base URL - not used in design mode, but required by build
VITE_API_BASE_URL=/api/v1

# File upload limits
VITE_MAX_FILE_SIZE=52428800
VITE_SUPPORTED_FORMATS=pdf,docx,pptx,txt

# Design Mode - ENABLED for design deployment
# This bypasses authentication and uses mock data
VITE_DESIGN_MODE=true
