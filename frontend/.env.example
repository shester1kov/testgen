# Frontend Environment Configuration

# API Base URL - use relative path to leverage Vite proxy
# This ensures cookies work correctly by keeping same-origin
VITE_API_BASE_URL=/api/v1

# File upload limits
VITE_MAX_FILE_SIZE=52428800
VITE_SUPPORTED_FORMATS=pdf,docx,pptx,txt

# Design Mode - bypass authentication for UI design tools (Anima, Figma)
# Set to 'true' to allow free access to all pages without authentication
# WARNING: NEVER set to 'true' in production with real backend!
#
# When enabled:
#   - All pages accessible without login
#   - No authentication checks
#   - No API requests to backend
#
# Usage:
#   ./scripts/enable-design-mode.sh   - Enable design mode
#   ./scripts/disable-design-mode.sh  - Disable design mode
#
# See: DESIGN_MODE.md for detailed documentation
VITE_DESIGN_MODE=false
